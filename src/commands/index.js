const PlaylistCommand = require('./playlist');
const Playlist2TextCommand = require('./playlist2text');
const PlaylistFromTextCommand = require('./playlistFromText');

module.exports = {
    PlaylistCommand,
    Playlist2TextCommand,
    PlaylistFromTextCommand
}